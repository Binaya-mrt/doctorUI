import 'package:flutter/material.dart';

class AppoinmentPage extends StatelessWidget {
  const AppoinmentPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('appinment'),
      ),
    );
  }
}
